name: "Upload IPFS Readme"
on:
  push:
    branches:
      - 'ipfs'

jobs:
  upload-ipfs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Upload to IPFS
        uses: aquiladev/ipfs-action@v0.3.1
        id: upload
        with:
          path: ./artifact
          service: pinata
          pinataKey: d43b3d2f82df4141bbf9
          pinataSecret: f53b53d24479b0081425aed4d1fd5ef3e9e4acb5f37c68b4aca11cadae44bd77
          pinName: Readme Test
          verbose: true
